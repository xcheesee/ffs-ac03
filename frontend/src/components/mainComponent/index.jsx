export default function MainComponent({ data }) {
    return (
			<div style={{fontSize: '64px', display: 'flex', justifyContent: 'center', alignItems: "center", height: "100vh"}}>{data}</div>
    )
}